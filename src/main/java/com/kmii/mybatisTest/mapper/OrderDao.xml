<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.kmii.mybatisTest.dao.OrderDao">
 	<select id="userOrderListDao" resultType="com.kmii.mybatisTest.dto.UserOrderDto">
	 	
		SELECT u.userid, u.username, o.orderid, o.ordername
	 		FROM springdb.usertbl u
	 			INNER JOIN springdb.order o ON u.userid = o.userid
	 				WHERE u.userid= #{param1}
	 	
 	</select>
 
 
 
 </mapper>